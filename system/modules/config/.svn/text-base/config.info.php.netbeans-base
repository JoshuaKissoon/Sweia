<?php

    /*
     * Module that handles all site configuration operations
     */

    $configinfo = array(
        "title" => "Site Configuration",
        "description" => "Module that handles all the Site configurations",
    );

    $configinfo['permissions'] = array(
        "access_config_panel" => "Access Admin Panel",
        "view_modules" => "View Modules",
    );

    $configinfo['urls'] = array(
        "config" => array("permission" => "access_config_panel"),
        "config/modules" => array("permission" => "view_modules"),
    );